import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["flagcdn.com", "source.unsplash.com", "cdn.pixabay.com", "images.unsplash.com"],
  },
};

export default nextConfig;
